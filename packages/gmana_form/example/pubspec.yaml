name: gmana_form_example
description: Demonstrates gmana_form fields, validation, and loading submit controls.
publish_to: "none"
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  gmana_form: ^0.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
