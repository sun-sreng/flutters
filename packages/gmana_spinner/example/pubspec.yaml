name: gmana_spinner_example
description: Demonstrates the gmana_spinner loading widgets.
publish_to: "none"
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  gmana_spinner: ^0.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
