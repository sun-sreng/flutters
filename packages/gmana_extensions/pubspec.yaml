name: gmana_extensions
description: Dart extension methods for Duration, String, num, Stream, Iterable, and validation utilities.
version: 0.0.1

topics:
  - dart
  - extensions
  - utilities

environment:
  sdk: ^3.7.2

resolution: workspace

dependencies:
  gmana_predicates: ^0.0.1
  gmana_validation: ^0.0.1

dev_dependencies:
  gmana_lints: ^0.1.0
  lints: ^6.1.0
  test: ^1.30.0
