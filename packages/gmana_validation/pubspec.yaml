name: gmana_validation
description: Pure Dart typed validators and extensions for common input, identifier, network, and date formats.
version: 0.0.1
repository: https://github.com/sun-sreng/flutters/tree/main/packages/gmana_validation
issue_tracker: https://github.com/sun-sreng/flutters/issues

topics:
  - validation
  - either
  - form
  - extensions

environment:
  sdk: ^3.7.2

dependencies:
  gmana_functional: ^0.0.1
  gmana_predicates: ^0.0.1

dev_dependencies:
  gmana_lints: ^0.1.0
  lints: ^6.0.0
  test: ^1.30.0

resolution: workspace
