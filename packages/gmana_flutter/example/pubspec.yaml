name: gmana_flutter_example
description: Demonstrates gmana_flutter widgets, form helpers, color utilities, and theme-mode extensions.
publish_to: "none"
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  gmana_flutter: ^0.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
